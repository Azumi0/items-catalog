---
category: Management
---

# UserForm

The new-account screen: a login and the password the account starts with, both
48px fields with 16px text. Takes no props — everything it needs comes from
the create action. Ends with `FormActionBar` ("Utwórz konto"), and its host
screen passes `chrome={false}`.

```jsx
<UserForm />
```
