// Design-sync stub for src/app/actions/users.ts — see ./README.md.
import { noop } from './noop';

export const createUserAction = noop;
export const changePasswordAction = noop;
export const deleteUserAction = noop;
