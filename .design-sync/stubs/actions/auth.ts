// Design-sync stub for src/app/actions/auth.ts — see ./README.md.
import { noop } from './noop';

export const logoutAction = noop;
export const loginAction = noop;
export const setupFirstUserAction = noop;
